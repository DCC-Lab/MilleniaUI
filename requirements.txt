# Millennia eV control GUI dependencies
mytk>=1.6.0   # RemoteControllable / remote RPC server
pyserial
# PyHardwareLibrary is not on PyPI; install from the DCC-Lab GitHub repo:
#   pip install git+https://github.com/DCC-Lab/PyHardwareLibrary.git
# (installs as the importable package "hardwarelibrary")
